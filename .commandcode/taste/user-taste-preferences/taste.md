- Wants consistent styling on all form inputs across pages — when one input has a border class (e.g. `border border-gray-300`) and another (e.g. password fields missing the `border` width class) renders borderless, all inputs should be made consistent so no form field stands out; likewise input heights/padding (e.g. `px-4 py-2.5`) must match across fields so no input renders shorter or taller than its peers. Confidence: 0.84- Iteratively verifies fixes: run targeted tests via `--filter` first, then the full suite, and check `php artisan route:list` after changes to catch routing/registration regressions. Confidence: 0.78
- Wants consistent border styling on all form inputs across pages — when one input has a border class (e.g. `border border-gray-300`) and another (e.g. password fields missing the `border` width class) renders borderless, all inputs should be made consistent so no form field stands out with a missing border. Confidence: 0.82- Uses card-based UI components (with avatars, images, action buttons like "buy"/"add to cart") for team members, services, and similar listings. Confidence: 0.85
- Wants prototype/dummy frontend pages properly enhanced for UX/UI (made data-driven and polished, following the project's design system) rather than left as hardcoded placeholders. Confidence: 0.85
- Expects production-ready, professional codebase quality (strives for 100/100). Confidence: 0.95
- Rejects AI-generated fluff/over-engineering in names and code ("no ai slop"); prefers clean, direct, human-readable naming — e.g. naming a feature simply "Email" rather than AI-flavored variants. Confidence: 0.80
- Actively wants the agent to identify and list brutal flaws in existing projects before improving them, documented as a structured report file (e.g., LMS_PROBLEM.md) with file:line references, severity levels (High/Medium/Low), and module-based categorization. Confidence: 0.85
- When auditing, wants findings verified against actual code/routes/models (attribute names, view existence, JS variables) before being committed to the report — "check what is broken," not guess. Confidence: 0.80
- Wants problem reports to include a prioritized "top critical" list and a "quick fixes" (low-effort, high-impact) section so flaws are actionable, not just exhaustive. Confidence: 0.80
- Requests project readiness status reported as a percentage out of 100. Confidence: 0.85
- Wants CHANGELOG.md and TODO.md maintained in projects. Confidence: 0.85
- Implements auto-advance navigation: when one item (e.g., course video, class) completes, automatically proceed to the next item wherever similar sequential logic exists. Confidence: 0.90
- Prefers shorter, streamlined forms (e.g., student admission forms) — keeps only fields that matter and make sense, removing school-style fields (family, religion, marital status, blood group, multi-part address). Confidence: 0.85
- Makes frontend UI elements role- and auth-context-aware: buttons/links (Admission, "Proceed to Enrol", "Direct Enrol") render different destinations or are hidden based on auth state — guests see public actions (Admission), students see student-specific actions (e.g. "Enroll in Course"), and admins/teachers see neither since they use the dashboard. Confidence: 0.85
- Wants scroll-based animations, smooth/spring transitions on landing pages and throughout the site. Confidence: 0.85
- Prefers draggable carousels that work on both desktop (mouse) and mobile (touch), handling page scrolls correctly. Confidence: 0.80
- Uses Unsplash (or similar) images to fill missing image placeholders (e.g., course images on landing pages) for a professional look. Confidence: 0.85
- Uses card-based UI components (with avatars, images, action buttons like "buy"/"add to cart") for team members, services, and similar listings. Confidence: 0.85
- Wants button text and other UI elements properly centered/aligned (button text, page-hero titles/descriptions, inline editors). Confidence: 0.85
- Prunes irrelevant academic/school features from LMS dashboards, keeping only course-selling-related items (Exams, Accounts, Inventory, Reports, Attendance) while distinguishing course-LMS-relevant modules from school/class-legacy ones. Confidence: 0.85
- Wants problem/flaw audit reports scoped to only the pages currently visible/used in the app (sidebar + frontend + student portal). Hidden/dead pages (e.g. SMS, groups, attendance sub-pages) should be segregated into a separate "not in use / reference" section at low priority rather than mixed into the priority findings list. Confidence: 0.85
- Payment systems follow an admin-verification workflow: student submits transaction ID and details, admin reviews in a table, notifications communicate between parties, admin approves, then access is granted. Confidence: 0.90
- Admin panels include multi-field search (name, number, batch, area, blood group) and dropdown/toggle filters (e.g., online vs. offline students, online/offline views). Confidence: 0.85
- Wants admin dashboard pagination to show sufficient items per page (complained about 'too less pages' limiting admin listings). Confidence: 0.70
- Prefers plan-first analysis: when asked to review and categorize items, says "don't do anything and just check" and "we need to plan" — expects the agent to analyze, enter plan mode, and present a categorized plan before making changes. Confidence: 0.90
- Wants dashboard/navigation items explicitly categorized as course-LMS-relevant vs school/class-legacy, with a clear list of what to remove, before implementation decisions are made. Confidence: 0.85
- Rebrands existing projects with client-specific identity (name, theme colors, logo, favicons) everywhere — no remnants of previous brand. Confidence: 0.90
- Deployments (Render, Vercel) must render all dashboard and frontend pages correctly — no broken/missing pages. Confidence: 0.85
- Wants deployment speed optimized — complained that deploys take 10+ minutes (repeatedly: "Again"). Root causes addressed included full re-seeding on every boot, bcrypt cost, and uncached Docker layers; prefers solutions that make deploys and seed runs fast. Confidence: 0.90
- Previously complained about "too less pages" with too few reachable sections, but now distinguishes course-LMS-relevant modules (which should all be exposed) from school/class-legacy modules like Exams, Accounts, Inventory, and Communication (which should be pruned). Confidence: 0.55
- Expects tasks fully completed and verified (tests green + Blade compiles) before accepting — repeats "please complete this" until verification confirms the fix works, not just a summary. Confidence: 0.85
- Prefers root-cause fixes over band-aids — asks to "fix it correctly" and accepts deep tracing through test/env tooling (e.g. CSRF/APP_ENV precedence) to the real cause. Confidence: 0.80
- Prefers graceful empty states (HTTP 200 with an empty-state message) over hard 403 Forbidden responses when a user lacks required profile data, reserving 403 for genuine authorization/ownership violations only. Confidence: 0.70
- Wants completed, verified work committed to git with a descriptive commit message and pushed to the remote as part of the standard completion workflow. Confidence: 0.85
- Operates with multiple GitHub accounts; expects the agent to use `gh auth switch` to the repo-owner account before pushing to a repo owned by a different account, then switch back to the default account afterward so it stays as the default. Confidence: 0.9
- Operates on Windows — expects Windows file paths and cmd/PowerShell-compatible shell commands; avoids bash-specific constructs such as heredocs. Confidence: 0.9
- Prefers commenting out navigation/UI elements (rather than deleting them) when they need to be hidden, preserving the code so they can be re-enabled later. Confidence: 0.85
- Wants the production (Render) database to match the local database exactly — same content/data, not just same code. When seeders only populate auth in production while dev seeds the full catalog, the live site ends up empty; the production seeder must seed the complete dataset. Confidence: 0.9
- Seeder scripts must be idempotent and non-destructive in production — uses updateOrCreate/findOrCreate instead of truncate, so redeploying/restarting the container never wipes existing production data. Confidence: 0.85
- Comfortable providing API keys (deployment-platform, third-party services like Brevo email, or otherwise) directly to the agent so it can configure and inspect the live environment. Confidence: 0.75
- Rejects unnecessary infrastructure additions (e.g. persistent disks) that weren't requested or needed — if the existing ephemeral setup works, do not invent "imaginary" complexity. Prefers minimal solutions that fix actual problems. Confidence: 0.90
- Prefers UI components (selects, inputs, etc.) to use theme color tokens (e.g., border-primary, text-primary, focus:ring-primary) rather than hardcoded gray color classes for visual consistency across the dashboard. Confidence: 0.75
- Expects image/file upload previews (logo, favicon) to display the currently-stored value by resolving relative/brand asset paths (e.g., images/brand/...) to full URLs rather than showing broken images. Confidence: 0.70
- Wants color picker controls to support two-way sync between the visual picker and hex text input, with hex validation. No separate preview swatch is desired since the native color picker already displays the color. Confidence: 0.70
- Uses Brevo (their established ESP) to send transactional email notifications to students on key lifecycle events — admission application received, payment approved, and certificate issued/emailed — rather than relying only on in-app notifications. Confidence: 0.85
- Wants certificate management to be functional end-to-end: able to update certificate fields (grade, issue date, status) and email the certificate (with a verification link) directly to the student. Confidence: 0.80
- Certificate generation should be template-driven and admin-managed: superadmins create/edit templates from the dashboard (sidebar page) with file uploads (background, logo, signature), set a default template, and issued certificates render from that template using real student/course model data (student name, course name, grade, certificate number, verification code, issue date). Confidence: 0.85
- Wants comprehensive "brutal" end-to-end test suites written and run across every visible feature (students, courses, videos, announcements, payments, certificates, services, admission), exercising real flows (auth CSRF, real DB, real email provider) rather than mocks, and wants the per-feature pass/fail results explicitly reported back. Confidence: 0.85
- Keeps a single canonical logout location — removes duplicate Sign out / signout actions from header dropdowns when a logout control already exists elsewhere (e.g. sidebar footer), avoiding UI duplication. Confidence: 0.9
- Wants UI consistency across all pages: dropdowns must populate and render their options, checkboxes must show the accent/green checkmark, and action buttons that should navigate must render as links — broken-on-specific-pages behavior is unacceptable. Confidence: 0.80
- Icons and supporting text must remain visibly contrasting against their container — an icon's color must not match its background color, otherwise the icon is invisible (e.g. an emerald-tinted background with an icon forced to the same green). Prefers letting the standard Tailwind color palette resolve so icons always contrast their backgrounds. Confidence: 0.85
- Icons must render at a consistent size across all pages and components (sidebar, stat cards, dropdowns, buttons, etc.) rather than a mix of sizes. Confidence: 0.85
- Sidebar accordion: when a child item is active, the entire accordion block should receive a light primary tint while the selected item uses solid dark primary with contrasting foreground — a two-tier color scheme so the active item is visually distinct from its open parent context. Confidence: 0.90
- Admin dashboard profile dropdowns should include a Home/Frontend link (pointing to the site root) so admins can navigate back to the live site directly from the dashboard header. Confidence: 0.85
- Seeders must populate rich sample data (students, enrollments, payments, etc.) so dashboards and list views are non-empty during review/testing instead of showing blank sections. Confidence: 0.75
- When a bug is reported on one page, proactively audit ALL other visible pages (dashboard + frontend + student portal) for the same class of failure and fix them all, not just the single reported page. Confidence: 0.85
- Fail-loud on missing seed data: code paths that assign roles (e.g. student/teacher creation, public admission) should abort with a clear error if the required role row is absent, rather than silently creating broken roleless accounts. Confidence: 0.85
- Verify fixes against a fresh/uncached state (clear view cache, don't rely on cached compilation) to catch production-only errors that local development's view cache masks. Confidence: 0.80
- For UI inputability bugs, treats "field present in HTML source" as insufficient proof of a complete fix — insists on verifying actual runtime interactivity (typing/focus), since overlays, z-index stacking, pointer-events, or scroll-reveal/opacity scripts can leave a field visible but untypeable. Confidence: 0.70
- Wants to review changes locally before any git push — "don't push it until I say" — the agent should leave work uncommitted/in the working tree for the user to inspect locally and only commit/push once explicitly approved. Confidence: 0.92
- Prefers clean, focused visual editors with no auxiliary "tips/help" side panels — removes the right-side Design Tips panel so the canvas/workspace fills the available width. Confidence: 0.80
- Prefers double-click-to-edit text inline directly on the canvas in template editors (not only numeric X/Y inputs) — a focused inline editor positioned exactly over the element, with cursor auto-focused/selected and editable via click or Enter/Escape to commit/cancel. Confidence: 0.85
- Prefers variable-picker buttons to show the plain key name (e.g. `student_name`) rather than wrapped template syntax (e.g. `{ student_name }`) so the actual token string is what's visible and inserted. Confidence: 0.90
- Prefers ring-based element selection (a `ring`/`outline` directly on the selected element) over a separate floating dashed-border overlay rectangle; also expects grab/move cursor feedback and clean (non-jumping) drag behavior in editors. Confidence: 0.78
- Prefers dragging elements from a sidebar/palette directly onto the live preview canvas and dropping them at the correct scale-aware coordinates (mouse position converted to template units accounting for canvas zoom), not only numeric input placement. Confidence: 0.82
- Maintain a permanent cross-page rendering smoke test that hits every visible dashboard and student page and asserts HTTP 200, as a regression net for 500s / broken relations / missing views. Confidence: 0.85
- Dashboard summary/stat cards (e.g. total students, teachers, revenue, attendance) should be clickable and navigable — links that take the admin to the relevant list page — rather than static decorative elements; no dummy/non-functional cards. Confidence: 0.85
- Dashboard charts must display real data backed by correctly-seeded data: seeders must populate the specific columns/fields the chart queries filter on (e.g. a revenue chart that filters on `payment_date` must have that column seeded, not just `submitted_at`), and seeders must set the timestamp column(s) the chart actually groups by (e.g. `created_at` must match the spread `payment_date`, otherwise the monthly chart collapses to one month). Chart result caches must be cleared so seed fixes are reflected. Confidence: 0.85
- Prefers live rendered previews (scaled thumbnails) in list/index views instead of placeholder icons/images, so configurable items like certificate templates show their actual appearance with sample data rather than a stock placeholder. Confidence: 0.90
- Wants template editors to support insertable dynamic variables for user/content-specific data (e.g. student name, course name, grade, certificate/verify number, issue date) via a picker the user can drop into text elements, so generated outputs auto-fill with real model data. Confidence: 0.85
- Wants feature-rich, granular per-element editing controls in visual editors (text: font, font size, color, bold/italic/underline, letter-spacing, alignment, X/Y position, box width, opacity; images: source field, width, height) with live-as-you-type preview updates reflecting changes immediately. Confidence: 0.85
- Wants direct canvas drag-and-drop in visual editors: elements must be draggable/movable on the live preview itself (scale-aware so they land where dropped), click-to-select with a visible selection outline, and deletable via the Del/Backspace key or a trash control — not just editable via numeric X/Y inputs. Confidence: 0.85
- Wants a template-level background opacity control (slider) in the certificate editor that sets how transparent the background image is, applied in the editor preview and on issued certificates (distinct from per-element opacity). Confidence: 0.85
- Prefers a compact/constrained live preview canvas in visual editors rather than a full-size preview. Confidence: 0.80
- Template listing cards should be clickable to navigate directly to that item's design/edit page. Confidence: 0.85
- Prefers reduced bcrypt hash cost (rounds=4) for seeded/demo account passwords to keep seeding and test runs fast, while retaining full-cost hashing for production secrets set via env vars. Confidence: 0.80
- Prefers Docker multi-stage builds with dependency lockfile layer caching (copy composer.json/lock and package.json/lock first, run installs, then copy source) and `npm ci` over `npm install`. Additionally ensures `.dockerignore` does NOT exclude lockfiles (`package-lock.json`, `composer.lock`) — they are required in the build context for reproducible `npm ci` / `composer install`. Confidence: 0.85
- Prefers conditional database seeding in the container entrypoint — run `db:seed` only when the database is empty (first boot), so subsequent restarts skip re-seeding. Confidence: 0.75
- Does not want temporary/scratch scripts or unused binary assets (e.g. font files) swept into git commits; expects clean, focused commits containing only relevant files. Confidence: 0.80
- Removes school/academic-era content (EIIN, school code, registration numbers, SSC-exam notices) from public chrome like the top-bar ticker and replaces it with course-LMS relevant content (programs, location, admissions info). Confidence: 0.80
- Prefers a single source of truth for entity lifecycle status (e.g. `admission_status` pending/approved/rejected) that is kept in sync with related fields (`status`, `user.is_active`) everywhere it is written — admission submission, admin store/edit, batch assignment, payment approval — so every page shows the same state. Confidence: 0.85
- Status badge rendering must be identical across every page that shows the same entity (All Students, Admission Applications, Batch Assignment, student profile) — no page should derive status differently (e.g. one page using `status`, another guessing from `batch_id`). Confidence: 0.90
- Admin workflows need explicit approve/reject actions (with account activation/deactivation and a notification email) on the page where the entity's applications are reviewed, so the status flow is actionable end-to-end. Confidence: 0.85
- Edit/create forms for the same entity should mirror each other's fields (both course-LMS trimmed), and editing must sync linked records (user name/email, lifecycle status) — not just save the row. Confidence: 0.85
- For tests asserting that an email/reset-link/notification is delivered on an action (e.g. student approval), prefers `Notification::fake()` with `assertSentTo`/`assertNotSentTo` for deterministic, offline-friendly verification rather than real mail delivery; and avoids tests that hit external APIs (e.g. uncompromised-password checks), since they're flaky in CI and test framework behavior rather than the app's actual fix. Confidence: 0.78
- For customer support tickets written in a non-English language (e.g. Bangla), responds to the ticket/recipient in that language so the explanation reaches them in their own language, rather than defaulting to English. Confidence: 0.72
- When validating passwords in this Laravel 12 codebase, prefers the fluent `Password` rule object (e.g. `Password::min(12)->mixedCase()->numbers()->symbols()`) over string-style rules, since string rules like `mixedCase` are not registered validators. Confidence: 0.80
- Guards credential-provisioning side-effects (password-reset links/emails) behind an account state flag (`must_change_password`) so they are only sent to accounts that don't already have a usable password — a reset link never overrides or re-sends for an account whose creator already set its password. Confidence: 0.85
- Prefers letting the creator (applicant on the public admission form, or admin on the add-student form) set a student's login password directly via an optional password field; only falls back to generating a random password plus an emailed reset link when no password is supplied — avoids email-dependent credential delivery where avoidable. Confidence: 0.78
- Uses the project's custom test runner `php tests/run-tests.php` (with `--filter` for targeted runs before the full suite) instead of `php artisan test` / PHPUnit defaults. Confidence: 0.82
- Debugs failing tests with `$this->withoutExceptionHandling()` to surface the real exception, then removes it for the committed test. Confidence: 0.70
- When a Blade component (e.g. `x-ui.password-input`) does not exist, falls back to raw HTML inputs whose classes match the existing form/field style rather than creating a new component. Confidence: 0.75
- On Windows (no heredoc support), writes multi-line commit messages to a scratch file outside the repo and runs `git commit -F <scratchfile>` rather than `git commit -m`. Confidence: 0.70
- Iteratively verifies fixes: run targeted tests via `--filter` first, then the full suite, and check `php artisan route:list` after changes to catch routing/registration regressions. Confidence: 0.78
